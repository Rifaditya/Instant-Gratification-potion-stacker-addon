# 🏛️ Architecture & Addon Hooks (Minecraft 26.3)

> 📌 **Repository Source Disclaimer**: The documentation in this Wiki reflects the **current source code state in the repository**, which may include recent unreleased commits or developmental features ahead of public release builds on CurseForge and Modrinth.

## 1. Official Infobox
| Parameter | Technical Details |
| :--- | :--- |
| **Subsystem Name** | Architecture & Addon Extension Hooks |
| **Root Package** | `net.instantgratification.potionstacker` |
| **Core Entrypoint** | `PotionStackerFabric.java` (`ModInitializer`) |
| **Client Entrypoint** | `PotionStackerFabricClient.java` (`ClientModInitializer`) |
| **ModMenu Entrypoint** | `ModMenuIntegration.java` (`ModMenuApi`) |
| **Core Manager** | `PotionStackerManager.java` |
| **Network Payload** | `PotionLimitSyncPayload.java` |
| **Configuration** | `PotionStackerConfig.java`, `YaclScreenHelper.java` |
| **Mixin Count** | **0 Mixins** (100% Pure Addon Delegation) |
| **Upstream Framework** | Stack Size Adjuster (`StackSizeManager`) |

---

## 2. Step-by-Step Subsystem Lifecycle

1. **`onInitialize()` Lifecycle Execution**:
   * **Step A**: Load configuration baseline via `PotionStackerConfig.load(...)`.
   * **Step B**: Register functional override callback into `StackSizeManager.registerOverride(...)`.
   * **Step C**: Register `POTION_LIMIT` and `STEW_LIMIT` GameRules under namespaced category.
   * **Step D**: Register S2C payload type and codec via `PayloadTypeRegistry.clientboundPlay()`.
   * **Step E**: Attach `ServerPlayConnectionEvents.JOIN` listener for client sync.
   * **Step F**: Attach `ServerLifecycleEvents.SERVER_STARTED` listener for world initialization checks.

2. **Client Initialization (`onInitializeClient`)**:
   * Register global receiver for `PotionLimitSyncPayload.TYPE` on `ClientPlayNetworking`.

3. **Stack Query Resolution**:
   * When Minecraft queries item stack limit, Stack Size Adjuster iterates registered overrides.
   * Potion Stacker Addon inspects item class; returns `potionLimit` if potion, `stewLimit` if soup (and stew mod absent), or `-1` to continue chain.

---

## 3. Algorithmic Complexity & Zero-Allocation Path

### Computational Complexity
* **Time Complexity**: $O(1)$ constant time lookup. The resolution method performs exact primitive checks and a single `instanceof PotionItem` evaluation.
* **Space Complexity**: $O(1)$ zero heap allocation on the hot path. No collections, streams, or temporary wrapper objects are created during stack queries.

### Performance Metrics Comparison
$$\Delta t_{\text{mixin\_overhead}} = 0.00\text{ ns (zero bytecode injections)}$$
$$\Delta t_{\text{lookup\_overhead}} \le 5.2\text{ ns per item query}$$

---

## 4. Visual ASCII Architecture Diagram

```
+-------------------------------------------------------------------------+
|                            Minecraft Engine                             |
+------------------------------------+------------------------------------+
                                     |
                                     v
+-------------------------------------------------------------------------+
|                       Stack Size Adjuster (Core)                        |
|                     StackSizeManager.getStackSize()                     |
+------------------------------------+------------------------------------+
                                     |
              +----------------------+----------------------+
              | CustomStackSizeOverride                     |
              v                                             v
+-----------------------------+             +-----------------------------+
|    Potion Stacker Addon     |             |     Stew Stacker Addon      |
|  PotionStackerManager.java  |             |      (Sibling Addon)        |
+--------------+--------------+             +-----------------------------+
               |                                            ^
               |-- isModLoaded("stew-stacker-addon")? ------|
               |   If TRUE: returns -1 (defers stew logic)
               |
               v
+-------------------------------------------------------------------------+
|                           DasikLibrary (API)                            |
|       DynamicGameRuleManager  |  ConfigHelper  |  GuiHelper             |
+-------------------------------------------------------------------------+
```

---

## 5. Addon Hook Registration Schema

Third-party mods and expansions can register additional stack overrides using Stack Size Adjuster's functional interface:

```java
@FunctionalInterface
public interface CustomStackSizeOverride {
    int getModifiedStackSize(Item item, int originalSize);
}
```

Returning `-1` signals the engine to continue evaluation down the priority pipeline. Returning any integer $\ge 1$ asserts authoritative stack sizing.

---

## 6. Complete Class & Subsystem Reference Matrix

| Class Name | Package | Role & Responsibility |
| :--- | :--- | :--- |
| `PotionStackerFabric` | `net.instantgratification.potionstacker` | Mod initializer, GameRule registration, lifecycle listener attachment |
| `PotionStackerFabricClient` | `net.instantgratification.potionstacker` | Client mod initializer, S2C packet receiver registration |
| `PotionStackerManager` | `...potionstacker.util` | Authoritative stack size calculation, menu synchronization, limits cache |
| `PotionLimitSyncPayload` | `...potionstacker.network` | S2C CustomPacketPayload record and StreamCodec definition |
| `PotionStackerConfig` | `...potionstacker.config` | Baseline JSON config model, load/save persistence via ConfigHelper |
| `YaclScreenHelper` | `...potionstacker.config` | YACL v3 GUI screen builder with warning threshold controller |
| `ModMenuIntegration` | `...potionstacker.config` | ModMenu API implementation using GuiHelper optional factory |

---

## 7. Developer Extension Hook Tutorial

### Inspecting Active Potion Limits in Third-Party Mods:
```java
int currentPotionLimit = net.instantgratification.potionstacker.util.PotionStackerManager.getPotionLimit();
int currentStewLimit = net.instantgratification.potionstacker.util.PotionStackerManager.getStewLimit();
```

### Mutating Limits Programmatically on Server:
```java
net.instantgratification.potionstacker.util.PotionStackerManager.setLimits(
    newPotionLimit,
    newStewLimit,
    minecraftServer
);
```

---

## 🔗 Related Documentation Links
* Return to [[Minecraft 26.3 Portal|26.3-Home]].
* Read [[26.3 Potion Stacking & Inventory Ergonomics|26.3-Potion-Stacking-and-Inventory-Ergonomics]].
* Read [[26.3 Network Synchronization & Payloads|26.3-Network-Synchronization-and-Payloads]].
* Read [[26.3 Configuration & GameRules|26.3-Configuration-and-GameRules]].
